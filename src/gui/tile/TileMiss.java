package gui.tile;

import javafx.scene.paint.Color;

public class TileMiss extends Tile{

    public TileMiss(int x, int y) {
        super(x, y);
        setFill(Color.WHITE);
        setStroke(Color.BLACK);
    }
}
